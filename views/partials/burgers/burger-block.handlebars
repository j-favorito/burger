<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Eat{{else}}Make Another{{/if}}
	</button>

	<button class="delete-burger" data-id="{{id}}">Remove Burger!</button>
</li>
